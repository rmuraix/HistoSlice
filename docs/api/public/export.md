# Export

Saves tile regions (and thumbnails/masks/metrics) to disk. Separate from `Slide`, so
saving logic never needs to know how slides are read.

::: histoslice.export.export_tiles

::: histoslice.export.ExportResult

::: histoslice.export.annotated_thumbnail
